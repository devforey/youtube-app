{
  "name": "youtube-app",
  "version": "1.0.5-beta2",
  "scripts": {
    "prepublish": "npm run build;",
    "dev": "concurrently \"rollup --config --watch\" \"gulp dev\"",
    "build": "rollup --config & gulp html",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "youtube-ui-lib": "^1.0.0-beta4"
  },
  "devDependencies": {
    "concurrently": "^4.1.2",
    "gulp": "^4.0.2",
    "node-sass": "^4.12.0",
    "rollup": "^1.23.0",
    "rollup-plugin-copy": "^3.1.0",
    "rollup-plugin-postcss": "^2.0.3"
  }
}
